# Editorial Queue Controls Release Validation

This documentation-only commit triggers a clean production build after the Vercel build-rate limit cleared.

Target application state:
- queue keyword search enabled;
- desk, priority, and workflow filters enabled;
- safe sorting enabled;
- queue control stylesheet imported;
- no publishing behavior changed.
